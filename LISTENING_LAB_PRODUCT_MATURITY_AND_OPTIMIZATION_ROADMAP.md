# Listening Lab 产品成熟度基线与优化路线图

## 1. 文档目的

本文档记录 Listening Lab 当前产品成熟度、已验证能力、主要风险和后续优化顺序。
后续迭代应以本文档为统一基线，优先补齐产品化基础设施，避免在稳定性、安全性和
用户体系尚未完善时继续无序增加功能。

文档基线日期：2026-08-19。

当前 Android 版本：`2.8.3 (versionCode 15)`。

当前阶段定义：**成熟 Beta，可用于个人长期使用和小范围内测，尚未达到公开商业化
发布标准。**

## 2. 当前成熟度结论

### 2.1 不同使用范围的完成度

| 使用范围 | 完成度 | 结论 |
| --- | ---: | --- |
| 个人长期使用 | 88% | 核心学习闭环完整，可以持续使用 |
| 小范围邀请内测 | 80% | 可以开展，但需要监控和人工运维兜底 |
| 公开商业化上线 | 60% | 用户、鉴权、安全、运维和正式发布体系仍不足 |

### 2.2 分维度成熟度

| 维度 | 完成度 | 当前判断 |
| --- | ---: | --- |
| 核心学习闭环 | 88% | 课程、听力、阅读、测验、口语、复习和进度基本完整 |
| 音频生成链路 | 85% | DeepSeek、OpenAI TTS、Whisper 和失败降级已真实验证 |
| Android 客户端 | 85% | 五个主页面、主题、多语言、在线更新和错误状态已具备 |
| 云端稳定性 | 80% | 服务可用，失败内容已从用户接口隐藏，但仍依赖外部 AI 服务 |
| 自动化测试 | 78% | 后端和 Android 单元测试较完整，真机自动化与 CI 仍不足 |
| 用户与数据隔离 | 55% | 有安装级 `clientId`，但不等同于账号、鉴权和租户隔离 |
| 安全与运维 | 50% | 当前仍使用 HTTP 明文 IP，监控、迁移和备份体系不完整 |
| 正式发布能力 | 65% | 支持自建 APK 更新，但尚不是应用商店级发布流程 |

## 3. 已验证的产品能力

### 3.1 学习闭环

1. 每日 5–8 分钟技术英语对话课程。
2. 自定义主题和来源课程生成。
3. MP3 播放、进度恢复、倍速、前后跳转和点击文本定位。
4. 对话级和单词级同步高亮。
5. 词汇学习、阅读理解、选择题和口语总结。
6. 口语录音、转写、多维评分和改进建议。
7. 学习进度、七日统计、课程复盘和每日复习队列。
8. 本机历史记录和云端进度同步。

### 3.2 内容与音频生成

1. DeepSeek 负责结构化课程脚本生成。
2. Kimi 保留为生成供应商兜底。
3. OpenAI TTS 负责语音合成，长文本会进行分段处理。
4. Whisper 负责单词时间戳；失败时可重试并降级为估算时间轴，避免已有 MP3
   因对齐失败而整体报废。
5. 生成失败原因按来源读取、脚本生成、音频合成和持久化阶段记录。
6. `FAILED` 内容在 Library、每日课程和推荐主题接口中均不可见。
7. 每日课程指向失败内容时，会移除失败引用并创建新的生成任务。

### 3.3 Android 交付

1. Today、Read、Practice、Library、Settings 五个主入口。
2. 中英文界面、深色模式和动态主题。
3. 云端不可用时保留本机历史和学习进度。
4. App 内检查版本、下载 APK、校验文件并调用系统安装确认。
5. Android 客户端会再次过滤 `FAILED`，避免旧服务或缓存数据进入 UI。

## 4. 当前验收基线

截至 2.8.3 版本修复，已获得以下本地测试与构建证据：

1. 后端完整单元测试与回归测试 94 项全部通过（含口语音频异步生成、评估失败真实状态返回、音频下载路径校验、TTS provider voice 严格校验）。
2. Android 单元测试 58 项全部通过（含 TTS voice 规范化与 provider 解析）。
3. Android Lint 及 Debug APK 构建成功 (`assembleDebug` 成功产出 `app-debug.apk`)。
4. 数据库完成 2.8.2 迁移脚本准备（`migrations/002_learning_enhancements_2_8_2.sql`）。

> [!NOTE]
> 注意：上述证据为本地单元测试与 APK 构建验证。云端真实部署 E2E 验证、Android 真机测试及 iOS 端实现属于后续发布/验证步骤，不应将单机单元测试与构建通过直接等同于整体发布验证通过。

## 5. 当前主要缺口

### 5.1 P0：公开内测前必须解决

1. **账号与鉴权**：当前 `clientId` 是安装标识，不是可信用户身份。
2. **数据隔离**：需要给用户数据增加明确所有权，并在所有查询和写入中强制校验。
3. **HTTPS**：当前 App 使用 `http://42.192.62.145:8080/`，传输未加密。
4. **密钥管理**：所有 AI 密钥必须只存在于云端，APK 中不得包含可用密钥。
5. **正式签名**：需要固定 release keystore、签名备份和升级兼容验证。
6. **监控告警**：需要生成成功率、生成耗时、供应商失败率、API 错误率和成本监控。
7. **数据库治理**：需要正式数据库迁移、自动备份、恢复演练和失败数据生命周期。

### 5.2 P1：公开测试前重点完善

1. 真机弱网、断网、后台切换、长时间播放和升级安装回归。
2. 自动化 CI，至少覆盖后端测试、Android 测试、Lint 和 APK 构建。
3. 云端与本机进度冲突解决策略。
4. 失败任务自动重试、最大尝试次数、死信队列和人工重跑入口。
5. 用户数据导出、删除、注销和录音隐私说明。
6. AI 调用配额、每日生成上限、成本保护和异常滥用限制。

### 5.3 P2：稳定后再增加

1. 新用户引导和分级测试。
2. 基于学习记录的个性化难度和主题推荐。
3. 更细的学习分析、留存指标和内容效果实验。
4. Play Store 或其他正式分发渠道。
5. iOS 真机签名、安装和与 Android 一致的验收矩阵。

## 6. 独立优化单元

每个单元必须可以独立开发、独立测试和独立回滚。一个单元未通过验收时，不应阻塞
其他不依赖它的单元。

### U1 用户身份与会话

- 目标：实现注册或第三方登录、短期访问令牌和可撤销刷新令牌。
- 后端范围：用户表、会话表、鉴权中间件、密码或第三方身份绑定。
- Android 范围：登录、退出、会话恢复和令牌安全存储。
- 独立验收：未登录请求被拒绝；令牌过期可刷新；退出后令牌不可继续使用。
- 状态：`PLANNED`。

### U2 数据所有权与用户隔离

- 目标：所有学习进度、词汇、口语、复习和用户创建内容具有 `user_id`。
- 后端范围：表结构扩充、数据迁移、查询强制作用域、跨用户访问测试。
- 独立验收：用户 A 无法读取、修改或推断用户 B 的任何私有数据。
- 依赖：U1。
- 状态：`PLANNED`。

### U3 HTTPS 与密钥收敛

- 目标：域名完成后启用 HTTPS，并确保 APK 不包含服务端 AI 密钥。
- 后端范围：TLS 终止、反向代理、证书续期和 HTTP 跳转。
- Android 范围：切换 HTTPS Base URL，移除明文网络许可和 AI 密钥字段。
- 独立验收：抓包不可读取业务明文；反编译 APK 找不到可用 AI 密钥。
- 状态：`BLOCKED_BY_DOMAIN`。

### U4 正式 APK 签名与更新安全

- 目标：建立可持续升级的 release 签名和安全更新流程。
- 范围：release keystore、签名备份、版本规则、SHA-256、强制更新和回滚策略。
- 独立验收：旧正式版本可覆盖升级；错误签名 APK 被系统拒绝；下载损坏时不安装。
- 状态：`PLANNED`。

### U5 生成任务可靠性与可观测性

- 目标：让失败可分类、可统计、可自动恢复。
- 范围：阶段状态、最大重试次数、指数退避、死信队列、重跑接口和监控面板。
- 独立验收：模拟 DeepSeek、Kimi、TTS、Whisper 和数据库故障，均能得到明确阶段、
  有限重试和预期终态；失败内容不会进入用户接口。
- 状态：`PARTIALLY_IMPLEMENTED`。

### U6 数据库迁移、备份与恢复

- 目标：数据库结构变更可追踪、生产数据可恢复。
- 范围：迁移工具、每日备份、保留周期、恢复脚本和失败内容归档策略。
- 独立验收：在隔离数据库中从最新备份恢复，记录数量和关键关联一致。
- 状态：`PLANNED`。

### U7 Android 真机与自动化回归

- 目标：建立每次发布前可重复执行的验收矩阵。
- 覆盖：首次安装、覆盖升级、弱网、断网、切后台、来电打断、长音频播放、
  高亮同步、录音权限、空间不足和安装权限。
- 独立验收：形成版本化测试报告，并明确自动化、模拟器和物理手机证据边界。
- 状态：`PLANNED`。

### U8 隐私、删除与成本保护

- 目标：满足真实用户使用所需的数据控制和成本边界。
- 范围：隐私说明、录音保存期限、导出/删除/注销、请求限流、每日配额和费用告警。
- 独立验收：用户删除请求可追踪完成；超过配额时返回明确状态且不会产生额外 AI 调用。
- 状态：`PLANNED`。

## 7. 推荐实施顺序

1. U1 用户身份与会话。
2. U2 数据所有权与用户隔离。
3. U3 HTTPS 与密钥收敛；等待域名期间可先完成密钥移除和反向代理准备。
4. U4 正式 APK 签名与更新安全。
5. U5 生成任务可靠性与可观测性。
6. U6 数据库迁移、备份与恢复。
7. U7 Android 真机与自动化回归。
8. U8 隐私、删除与成本保护。

在 U1–U7 完成以前，原则上不增加大型创意功能；只处理阻断学习闭环的缺陷和低风险
体验优化。

## 8. Beta 升级到 1.0 的退出标准

满足以下全部条件后，才将产品定义为可公开发布的 1.0：

1. 所有私有接口有服务端鉴权，跨用户隔离测试全部通过。
2. App 全部业务流量使用 HTTPS，APK 中无可用服务端密钥。
3. 使用固定 release 签名，至少完成一次旧版到新版的真实覆盖升级。
4. 连续 7 天课程生成成功率不低于 95%，生成耗时 P95 不超过 8 分钟。
5. 外部 AI 服务失败时具有有限重试和死信处理，不会无限等待或重复扣费。
6. Library、Today 和推荐主题中 `FAILED` 内容可见数量始终为 0。
7. 数据库自动备份运行正常，并完成至少一次恢复演练。
8. 关键 Android 真机矩阵通过，无阻断级崩溃、播放、录音或更新问题。
9. 具备隐私说明、用户数据删除能力、请求限流和成本告警。
10. 发布版本的后端测试、Android 测试、Lint、构建和真实云端冒烟测试全部通过。

## 9. 每次迭代的记录要求

每个优化单元完成后，在本文档或独立测试报告中记录：

1. 单元编号和实际范围。
2. 数据库、API、Android 和云端行为变化。
3. 独立测试命令及结果。
4. 模拟器、物理手机和真实云端验证边界。
5. 发布版本号、构建产物、部署时间和回滚位置。
6. 尚未解决的问题和下一单元依赖。

## 10. 变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-08-01 | 建立产品成熟度基线和 U1–U8 优化路线图 |
