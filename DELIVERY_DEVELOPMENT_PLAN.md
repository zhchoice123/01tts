# Listening Lab 产品优化 V1 开发计划与验收标准

## 1. 本轮目标

将 Listening Lab 从“功能已经较多的 Beta”收敛为一个能够持续使用的学习产品，优先打通：

```text
首次进入 → 选择学习目标 → 获得今日课程 → 完成五步训练 → 查看结果 → 进入复习
```

本轮成功标准不是增加更多 AI 模型，而是让用户第一次打开 App 后知道做什么、过程中不会迷路、完成后能看到下一步。

## 2. 当前产品基线

当前版本基线为 Android 2.8.4 / versionCode 16，已经具备：

- Today、Read、Practice、Library、Settings 五个一级入口。
- 今日计划、异步课程生成、课程库和学习进度同步。
- 阅读、听力、理解题、口语录音和口语反馈流程。
- TTS Provider 与音色选择，支持阿里云 CosyVoice 和 OpenAI TTS。
- 生词本、学习复习队列、课程复盘报告和 AnkiDroid 联动。
- Android 仅访问自有后端，不再在 APK 中保存第三方 API Key。

当前主要问题：

1. 首次打开缺少明确的目标、水平和每日时长引导。
2. Today 页面信息较多，主行动没有形成稳定的“开始/继续今日课程”路径。
3. 课程五步能力已经存在，但用户对当前步骤、下一步骤和完成条件的感知不够统一。
4. 异步生成、失败、断网、杀进程恢复等状态需要统一成产品语言。
5. TTS 选择和试听需要始终明确显示“当前 Provider + 当前音色”，避免试听内容与选择项产生歧义。
6. 产品设计文档仍有 Android 直连 AI 和保存 API Key 的旧描述，需要与当前后端代理架构对齐。

## 3. 产品范围

### 3.1 本轮纳入

- 三步首次使用引导。
- Today 首页主任务卡和课程状态统一。
- 课程内五步训练导航与断点恢复。
- 课程完成报告与复习入口。
- 失败、重试、取消、断网和重新进入行为。
- TTS Provider/音色的产品文案和试听反馈。
- 本地可验证的产品事件与质量验收记录，不引入第三方埋点 SDK。
- 更新产品设计文档、开发文档和测试用例。

### 3.2 本轮不纳入

- 新增 AI Provider 或新模型。
- Android 端直连 OpenAI、DeepSeek 或阿里云。
- 登录、多人账户、社交功能和公开排行榜。
- 全量离线课程、离线 AI 和完整 Room 数据迁移。
- 重写现有 FastAPI、Redis 或 TTS 服务架构。
- 重新设计视觉品牌；继续使用现有 Listening Lab 视觉系统。

如果后续发现必须修改后端接口，优先增加兼容字段或新接口，不直接破坏现有 Android 客户端契约。

## 4. 目标用户流程

### 4.1 新用户流程

1. 首次进入展示产品价值：每天用一篇技术英语内容完成阅读、听力、表达和复习。
2. 第一步选择目标：技术英语、日常英语、口语提升，默认选择技术英语。
3. 第二步选择水平：A2、B1、B2、C1，默认选择 B1。
4. 第三步选择每日时长：10、20、30 分钟，默认选择 20 分钟。
5. 完成引导后进入 Today，并只突出一个主按钮：`开始今日学习`。
6. 今日课程尚未准备好时，显示生成进度和预计下一动作，不显示空白页面。
7. 课程准备好后按五步完成：词汇热身、盲听、对照阅读、理解测试、口语总结。
8. 完成后进入课程报告，显示本次完成情况、生词、错题、口语反馈和复习入口。

### 4.2 老用户流程

1. 升级后保留现有语言、主题、TTS、课程历史和学习进度。
2. 不重复展示首次引导；只有本地没有引导状态时才展示。
3. Today 根据已有 `contentUuid`、学习进度和任务状态恢复“开始”或“继续”。
4. 老课程保持可打开；新流程只影响新建课程和 Today 入口。

## 5. 数据与状态契约

### 5.1 首次引导状态

新增本地非敏感设置，建议使用现有 Preferences/DataStore 边界保存：

```text
OnboardingState
  completed: Boolean
  goal: TECHNICAL_ENGLISH | GENERAL_ENGLISH | SPEAKING
  level: A2 | B1 | B2 | C1
  dailyMinutes: 10 | 20 | 30
  completedAt: ISO-8601 timestamp
```

要求：

- 不保存 API Key、密码或录音内容。
- 首次引导可以跳过，跳过后写入安全默认值，不能反复拦截用户。
- `completed` 只在用户点击完成后写入；中途退出再次打开可以继续引导。

### 5.2 Today 课程状态

UI 统一使用以下产品状态，不直接把后端技术状态原样展示给用户：

| 产品状态 | 条件 | 用户动作 |
|---|---|---|
| `EMPTY` | 当天没有计划或课程 | 生成今日课程 |
| `PROCESSING` | 任务已提交，文章或音频未完成 | 查看进度/稍后重试 |
| `READY` | 课程正文、题目和可播放音频齐备 | 开始今日学习 |
| `IN_PROGRESS` | 已有任意学习进度 | 继续今日课程 |
| `COMPLETED` | 五步均满足完成条件 | 查看课程报告/进入复习 |
| `FAILED` | 任务失败或不可恢复 | 查看原因/重新生成 |

后端已有的 `202` 异步任务、内容状态、学习进度和复盘接口优先复用，不为 UI 增加第二套状态事实来源。

### 5.3 五步完成条件

| 步骤 | 完成条件 | 失败或跳过规则 |
|---|---|---|
| 词汇热身 | 用户完成本次展示的词汇操作 | 没有词汇时显示“本节无新增词汇”，不得阻塞 |
| 盲听 | 音频有效播放并达到最小有效进度 | 音频失败可重试，不伪造完成 |
| 对照阅读 | 用户打开或完成正文阅读状态 | 阅读位置需要保存 |
| 理解测试 | 提交有效答案，保存正确数/总数 | 网络失败可重新提交，不能重复扣除或覆盖历史 |
| 口语总结 | 成功提交有效录音并得到转写或明确处理中状态 | 权限拒绝、空录音和服务失败必须可解释 |

课程报告必须基于真实记录；缺失数据显示“未完成/暂无数据”，不得生成虚假的分数、连续天数或反馈。

## 6. 开发任务拆分

### P0-1：产品契约和文档对齐

- [x] 更新 `LISTENING_LAB_PRODUCT_AND_TECHNICAL_DESIGN.md`，删除 Android 直连 AI、客户端 API Key 和 Android Keystore Provider Key 的旧方案。
- [x] 明确 Android → 自有后端 → AI/TTS Provider 的唯一生产链路。
- [x] 统一 Settings、README、隐私说明和开发计划中的产品文案。
- [x] 建立本计划中的状态表、五步完成条件和兼容策略。

退出标准：文档中不再出现与当前代码冲突的客户端密钥保存方案；产品、技术和安全文档使用同一套状态名称。

### P0-2：首次使用引导

- [x] 新增 `OnboardingState` 及持久化实现。
- [x] 新增三步引导页面和中途退出恢复。
- [x] 增加跳过入口，写入默认目标、水平和时长。
- [x] 完成后跳转 Today，不进入设置页或空白课程页。
- [x] 设计中英文文案、深色/浅色主题和无障碍描述。
- [x] 增加 ViewModel 单元测试；Compose UI 测试列入下一轮设备验收。

退出标准：新安装用户只看到一次引导；重启 App 后状态保留；升级老版本用户不被强制重新引导。

### P0-3：Today 首页主任务

- [x] 将 Today 顶部固定为“今日课程 + 预计时长 + 当前进度 + 一个主按钮”。
- [x] 将 Review Queue、趋势数据和课程报告放到主任务之后，避免抢占首要行动。
- [x] 将 `EMPTY/PROCESSING/READY/IN_PROGRESS/COMPLETED/FAILED` 映射为统一组件。
- [x] 所有状态提供明确的下一步：生成、等待、开始、继续、查看报告或重试。
- [x] 课程处理中支持手动刷新；刷新期间禁止重复提交同一生成任务。
- [x] 课程失败显示可理解的原因和重试动作，不展示堆栈或内部异常。

退出标准：新用户进入 Today 后无需猜测下一步；老用户可以从原课程位置继续；同一课程不会因重复点击产生重复任务。

### P0-4：课程内五步导航

- [x] 为五步增加统一步骤条、当前步骤、完成标记和下一步按钮。
- [x] 每个步骤完成后自动保存进度，并根据真实进度更新下一步提示。
- [ ] 用户离开课程后再次进入，回到最近一个未完成步骤。
- [x] 已完成步骤允许查看，不默认重复提交数据。
- [x] 口语步骤明确展示录音权限、录音中、上传中、处理中、反馈完成和失败状态。
- [x] 任何步骤没有数据时显示空状态，不把“无数据”误记为“完成”。

当前剩余：重新进入课程时已经恢复真实进度和“下一步”指示，但尚未自动滚动到页面内最近一个未完成区块，因此保留该项未验收。

退出标准：完成任一步后杀进程并重新进入，进度和下一步骤正确恢复；五步顺序不会因网络重试或旋转屏幕发生跳变。

### P0-5：课程完成报告与复习入口

- [x] 完成课程后展示课程标题、实际完成步骤、阅读/听力/答题/口语真实结果。
- [x] 展示本课已复习词汇数量、错题数量和返回词汇/答题区入口。
- [ ] 展示“重新练习口语”“查看错题”“加入复习”等明确动作。
- [x] 没有某项数据时显示暂无数据，不计算虚构总分。
- [x] 与现有 `lessonReviewState`、review queue 和 vocabulary 数据保持一致。

当前剩余：已提供“重新练习口语”“查看词汇与错题”“前往今日复习”；真正的“加入复习”写操作仍需后端接口，未用本地假状态代替。

退出标准：完成课程后用户能在一个页面找到本课反馈和下一次学习动作；报告数据可通过后端接口和本地状态抽样对账。

### P0-6：TTS 选择和试听体验

- [x] Provider 选择项明确显示 OpenAI / 阿里云 CosyVoice。
- [x] 音色项显示供应商、音色名和性别/口音说明，例如 `Aliyun · David · US male`。
- [x] 试听按钮文案包含当前选中音色，生成中禁用重复提交。
- [x] 试听成功后播放器标题或辅助文案显示服务端实际返回的 provider-qualified voice。
- [x] 试听失败显示服务失败、限流、超时或音色不可用的可理解提示。
- [x] 新课程请求始终携带当前选择，不因切换 Provider 遗留旧音色。

退出标准：分别选择阿里云 David、阿里云 Abby、OpenAI Nova 后试听，界面展示项、请求参数和返回音频三者一致。

### P0-7：异常、恢复和数据保护

- [ ] 统一处理无网络、超时、401/403、429、5xx、任务失败和音频下载失败。
- [ ] 对可重试错误提供重试；对不可重试错误给出下一步说明。
- [ ] 生成按钮、提交答案、上传录音等操作具备幂等或客户端防重复机制。
- [ ] 杀进程、后台恢复、屏幕旋转和返回键行为不丢失当前进度。
- [ ] 日志不得包含 API Key、Authorization Header、录音原文或完整用户内容。
- [ ] 公网服务不可用时，App 显示服务暂不可用，不展示假课程或假完成。

退出标准：按验收矩阵逐项模拟错误，用户始终能知道当前状态、数据是否保存以及下一步应该做什么。

### P0-8：质量与发布准备

- [ ] ViewModel 覆盖引导、Today 状态、五步恢复、TTS 选择和重试。
- [ ] Compose UI 覆盖新用户、老用户、处理中、失败、完成报告和 Settings。
- [ ] 后端覆盖重复生成、任务状态、学习进度、TTS voice 校验和报告数据。
- [x] 执行安全扫描、Python 测试、Android 单元测试、Lint 和 APK 构建。
- [ ] 使用新安装和旧版本升级两条路径进行真机验收。
- [ ] 记录版本号、APK SHA-256、测试设备、测试结果和未通过项。

## 7. 验收标准

### 7.1 产品功能验收

- [ ] 新安装后首次引导最多三步，完成或跳过后进入 Today。
- [ ] 首次引导重启后不重复出现；升级已有用户不被打断。
- [ ] Today 顶部只有一个主行动，且文案随状态切换为生成、开始、继续、查看报告或重试。
- [ ] 今日课程从准备到完成的状态变化可观察，不能一直显示全局 Loading。
- [ ] 课程五步按照定义完成并保存；缺少数据时不伪造完成。
- [ ] 课程完成后可以进入报告、错题、生词和复习入口。
- [ ] TTS Provider、音色、试听请求和试听结果一致。

### 7.2 恢复与异常验收

至少执行以下场景，并记录屏幕录制或截图与结果：

| 场景 | 预期结果 |
|---|---|
| 首次安装并完成引导 | 进入 Today，默认目标和时长正确 |
| 引导中途杀进程 | 重启后回到未完成的引导步骤 |
| 老版本升级 | 不显示首次引导，历史课程可打开 |
| 今日课程处理中退出 App | 重进后显示处理中，可刷新，不重复提交 |
| 课程生成失败 | 显示原因和重试按钮，旧数据不被覆盖 |
| 完成阅读后杀进程 | 重新进入回到听力或下一个未完成步骤 |
| 断网提交答案 | 明确提示未提交，恢复网络后可以重试 |
| 录音权限拒绝 | 不崩溃，说明如何重新授权 |
| 音频下载失败 | 播放器显示失败和重试，不标记听力完成 |
| 选择阿里云 David/Abby | 试听与课程请求保持对应音色 |
| 选择 OpenAI Nova | 试听与课程请求保持 `openai:nova` |
| 连续点击生成/提交 | 只产生一次有效请求或由服务端幂等处理 |
| 中文/英文切换 | 当前页面文案立即切换，状态和数据不丢失 |
| 浅色/深色切换 | 文本、按钮、进度和错误信息均可读 |

### 7.3 真机验收矩阵

至少覆盖：

- Android API 26 或更低端兼容设备。
- Android API 33，验证通知权限和录音权限。
- 当前 Android 主流 API 版本，验证更新、媒体播放和后台恢复。
- 小屏和大屏各一台或对应模拟器。
- Wi-Fi、移动网络、断网三种网络状态。

### 7.4 自动化验收

```bash
# Backend
cd 01tts-worker
python -m unittest discover -s tests -v

# Android
cd ../01tts-app
./gradlew clean testDebugUnitTest assembleDebug
./gradlew lintDebug

# Repository security
cd ..
bash scripts/security-scan.sh
git diff --check
```

自动化验收必须满足：

- 后端测试全绿。
- Android 单元测试全绿。
- Lint 无新增 error。
- Debug APK 构建成功；发布前另行构建 Release APK。
- 安全扫描没有密钥、私钥、Bearer Token 或受保护配置文件。
- 所有新增文案和状态测试通过。

## 8. 发布门禁

只有以下条件全部满足，才能把本轮版本发布给真实用户：

1. P0-1 至 P0-7 的退出标准全部满足。
2. 新安装流程和旧版本升级流程均通过真机验收。
3. 新建一节真实课程，最终达到可播放、可答题、可提交口语和可查看报告。
4. 至少完成一次断网、超时、任务失败和重复点击验收。
5. APK 不含第三方 Provider Key，后端日志不含密钥或完整录音内容。
6. 公网 API、音频地址、APK 版本号、大小和 SHA-256 可以相互核对。
7. 未验证的分支、设备或线上能力必须在发布记录中明确标注，不能用绿色构建代替真实产品验收。

## 9. 后续迭代顺序

P0 产品闭环完成后，再按以下顺序投入：

1. Library 搜索、筛选、收藏和下载。
2. 更细的听写、逐句跟读和口语单句复练。
3. Anki 复习体验和词汇掌握反馈。
4. 周报、学习趋势和真实留存指标。
5. 离线内容、数据导出和备份。

这些能力必须建立在“首次使用和每日课程闭环稳定”的基础上，不提前扩大本轮范围。
