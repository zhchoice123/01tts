# Listening Lab × Anki 每日学习产品需求文档

**状态：** V2.1 MVP 已实现并验证（2026-07-26）

**版本：** 1.0

**目标版本：** Listening Lab V2.1

## 1. 背景与问题

当前 Listening Lab 已支持云端生成英语文章、题目和 TTS，但词汇学习与手机上的 AnkiDroid 相互独立。用户需要在两个 App 间重复选词，且 Listening Lab 自建的复习周期会与 Anki 排程冲突。

本版本把 AnkiDroid 作为唯一词汇排程来源：Listening Lab 读取指定 Deck 中今天到期的卡片，用这些词生成一篇可读、可听、可答题的复习材料。服务器每天 05:00 另外准备新闻和热门话题，用户打开 App 即可开始学习。

本机 `word` 工具的可复用能力是：按 Deck 查重、写入结构化词卡、保存发音，并在桌面 Anki 不可用时暂存待同步内容。Android 版应提供相同的“明确成功/失败”反馈，但直接使用 AnkiDroid 本地接口，不依赖桌面 AnkiConnect。

## 2. 产品目标

1. 用户可以在 Settings 中绑定一个 AnkiDroid Deck，并看到安装、权限、Deck 和字段映射状态。
2. App 可以读取该 Deck 今天到期的词，但不修改 Anki 排程。
3. 用户可以用到期词生成一篇包含所有目标词的文章、TTS 和选择题。
4. 阅读中的核心词可查看 Anki 释义、播放读音，并跳转到 AnkiDroid 复习。
5. Listening Lab 中遇到的新词可单个或批量加入已绑定 Deck，且不会静默创建重复卡片。
6. 服务器每天 05:00 提前准备来源可追溯的新闻/热门话题课程。

## 3. 非目标

- 不在 Listening Lab 中实现新的间隔重复或“艾宾浩斯”算法。
- 不代替 AnkiDroid 的 Again/Hard/Good/Easy 复习界面。
- 不读取 AnkiWeb 账号、密码或云端集合。
- 不把整个 Deck 上传到服务器。
- 不抓取或公开分发付费新闻全文。
- 本版本不实现多用户、社交排名、积分商城或复杂 AI 对话。

## 4. 核心用户流程

### 4.1 首次绑定

用户进入 **Settings → Anki**：

1. App 检测 AnkiDroid 是否安装。
2. 请求 AnkiDroid 数据库读写权限。
3. 用户选择 Deck、笔记类型以及“单词/释义/例句/音频”字段。
4. App 执行只读测试并显示：Deck 名、卡片数、今日到期数和映射结果。
5. 配置只保存在手机，不上传 Anki 标识或排程数据。

若 AnkiDroid 未安装或拒绝权限，仍可使用 Listening Lab；“加入 Anki”和“到期词复习”显示可恢复的引导，不阻塞其他功能。

### 4.2 今日 Anki 复习课

Today 页面显示“Anki 今日复习”卡片：

- 已绑定且有到期词：显示数量和最多 30 个词的预览。
- 没有到期词：显示“Anki 今日已清空”，可选择最近新增词练习。
- 尚未绑定：显示“连接 AnkiDroid”。

用户点击“生成复习文章”后选择 CEFR、文章长度、主题偏好和语音。云端生成必须自然使用所有目标词，并返回：

- 250–500 词文章；
- 目标词高亮及上下文释义；
- 全文 MP3；
- 3–5 道选择题与解析；
- 1 个英文复述提示。

完成 Listening Lab 练习不会自动在 Anki 中回答卡片。用户点击“去 Anki 复习”后，由 AnkiDroid 自己记录评分和下次到期时间。

### 4.3 添加新词

阅读页词汇面板提供：

- **加入 Anki**：写入当前词；
- **加入本课词汇**：批量写入用户勾选的词；
- **在 Anki 中已有**：展示已有状态，不重复新增；
- 写入结果：成功、重复、权限不足、字段不匹配分别计数。

默认优先兼容本地 `word --rich` 使用的 `English Vocabulary` Deck 和 `英语单词模板(vocab配色)` 笔记类型，包括 `英语单词`、`英美音标`、`中文释义`、`英语例句`、`中文例句`、`vocabulary简明`、`vocabulary扩展`、`柯林斯星级`、`柯林斯解释`、`英语发音`。字段不同的 Deck 必须先完成映射。

### 4.4 热门话题与新闻

Read 页面提供两种来源并明确标识：

- **来源文章**：RSS/Atom 或允许访问的公开页面，保留来源、标题、发布时间、原文链接和短摘录；模型只负责筛选、分级改写和出题。
- **AI 推荐话题**：DeepSeek/Kimi 根据技术、体育、商业、科学、文化等兴趣推荐主题，再生成原创学习文章；不得标记为新闻。

每天 05:00 服务器为当天准备 3 个候选话题，至少包含 1 个来源文章。用户可以直接学习，也可以基于候选话题重新生成不同难度版本。

## 5. 功能需求

| 编号 | 需求 |
|---|---|
| FR-A01 | 检测 AnkiDroid 安装、权限和数据可用状态。 |
| FR-A02 | 列出 Deck，并保存一个默认绑定 Deck。 |
| FR-A03 | 自动识别常见字段 `英语单词`、`Front`、`Word`，无法识别时要求手动映射。 |
| FR-A04 | 使用 Anki 搜索条件 `deck:"..." is:due` 读取今日到期卡片。 |
| FR-A05 | 查询最多 30 个唯一目标词，并显示过滤或映射失败数量。 |
| FR-A06 | 单个/批量写入词卡；写入前查重，部分失败不得回滚已成功项。 |
| FR-A07 | 提供跳转 AnkiDroid 的复习入口；不代答、不改排程。 |
| FR-R01 | 提交到期词快照并异步生成文章、词汇标注、TTS、选择题和复述题。 |
| FR-R02 | 生成结果必须覆盖全部目标词；缺失时最多自动修复一次。 |
| FR-R03 | 相同日期、Deck 快照、难度和主题重复提交返回同一任务或已有结果。 |
| FR-N01 | 每日 05:00 拉取来源、去重、评分并创建 3 个候选话题。 |
| FR-N02 | 新闻卡必须显示来源和原文链接；AI 原创内容必须显示“AI 生成”。 |
| FR-N03 | 单个新闻源失败不影响其他来源和当天已有内容。 |
| FR-D01 | Today 首屏优先读取缓存并后台同步，不能因 Anki 不可用而整页失败。 |

## 6. 非功能需求

- **隐私：** 服务器只接收本次选中的词和可选释义，不接收完整 Deck、AnkiWeb 凭据、真实 note/card ID 或排程字段。
- **性能：** 本地 Deck/到期查询在 2 秒内完成；Today 缓存首屏在 1 秒内可见；生成任务 95% 在 120 秒内进入 READY 或明确 FAILED。
- **可靠性：** 05:00 调度幂等；进程重启后可补偿当天缺失内容；任务不得永久停留在 GENERATING。
- **兼容性：** 支持 App 当前 `minSdk 26`，并对“未安装、未授权、首次未配置、字段不兼容、AnkiDroid 版本差异”提供独立状态。
- **版权：** 默认只保存学习所需短摘录、来源元数据和 AI 改写版本；原文通过链接打开。
- **可访问性：** 中英文界面文案、屏幕阅读标签、可调字号和音频播放控制保持可用。

## 7. 数据与成功指标

不以虚构 XP 衡量学习效果。第一版记录：

- Anki 绑定成功率；
- 到期卡片读取成功率和映射失败率；
- 复习文章生成成功率、目标词覆盖率和耗时；
- 每日话题 05:00 前后准备成功率；
- 文章阅读完成率、题目正确率、音频完成率；
- “加入 Anki”成功/重复/失败数量。

Listening Lab 的答题结果只用于展示和内容难度建议，不改变 Anki 下次复习日期。

## 8. 分期范围

### MVP

- AnkiDroid 安装/权限检测、Deck 绑定和字段映射；
- 读取今日到期词；
- 生成到期词文章、MP3、选择题；
- 单词加入 Anki 与查重；
- 05:00 新闻/热门话题预生成；
- Today/Read 中展示来源和错误状态。

### 后续

- 多 Deck 组合、按标签过滤；
- Anki 卡片浏览/搜索深链；
- 英美音频写入 Anki 媒体库；
- 离线缓存整套复习课；
- 根据 Listening Lab 错题推荐“去 Anki 再复习”，但仍不改排程。

## 9. 产品验收标准

1. **绑定：** Given 手机安装 AnkiDroid，When 用户授权并选择 Deck，Then App 显示 Deck 名、今日到期数和有效字段映射，重启后仍保留绑定。
2. **拒绝权限：** Given 用户拒绝权限，When 打开 Today，Then 其他学习内容可用，Anki 卡片显示授权入口而非崩溃或无限加载。
3. **到期词：** Given Deck 中有 12 张 `is:due` 卡，When 刷新，Then App 去重并展示可映射词；数量与 AnkiDroid 搜索结果一致。
4. **复习课：** Given 10 个目标词，When 任务 READY，Then 文章包含全部词，MP3 可播放，至少 3 道选择题可提交并显示解析。
5. **排程边界：** Given 用户完成 Listening Lab 题目，Then Anki 卡片的 due、interval 和 reps 不发生变化；只有在 AnkiDroid 中回答后才变化。
6. **写入：** Given 5 个新词和 2 个重复词，When 批量加入，Then 结果显示 5 成功、2 重复，目标 Deck 中只新增 5 条。
7. **每日内容：** Given 调度被重复触发，When 查询当天候选，Then 只有一组有效结果，至少一项带来源 URL，失败源有日志但不阻塞成功项。
8. **来源标识：** Given 一项 AI 原创文章，Then UI 不显示新闻来源；Given 来源文章，Then UI 可打开原文并显示发布时间。

## 10. 上线前决策闸门

开发第一项必须是真机 AnkiDroid 兼容性验证。只有以下四项通过后，才冻结 Android 接口实现：

1. 当前手机 AnkiDroid 版本能授权 `READ_WRITE_DATABASE`；
2. 能列出 Deck 和笔记类型；
3. `deck:"目标 Deck" is:due` 查询结果与 AnkiDroid 卡片浏览器一致；
4. 能对测试 Deck 完成查重、新增和读取，不影响排程。

若官方 ContentProvider 在当前版本不能稳定读取到期卡片，MVP 降级为“用户在 AnkiDroid 分享/选择词后生成课程”，而不是自行读取数据库或复制 Anki 的排程算法。
